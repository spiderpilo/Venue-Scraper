# Define here the models for your scraped items
#
# See documentation in:
# https://docs.scrapy.org/en/latest/topics/items.html

import scrapy

"""
class VenueScrapeItem(scrapy.Item):
    # Scrapy Items: custom Python containers used to collect & organized
    # scraped data into a predefine,structured schema.

    # Data Fields (i.e: columns or JSON/dictionary keys):
    venue_id = scrapy.Field()
    venue_name = scrapy.Field()
    source_url = scrapy.Field()
    business_type_gold = scrapy.Field()
    experience_category_gold = scrapy.Field()
    incentive_category_gold = scrapy.Field()
    teaser_gold = scrapy.Field()
    description_gold = scrapy.Field()

    scraped_url = scrapy.Field()
    final_url = scrapy.Field()
    status = scrapy.Field()
    scrape_stage = scrapy.Field()

    page_title = scrapy.Field()
    text_chars = scrapy.Field()
    incentive_score = scrapy.Field()
    keyword_hits = scrapy.Field()

    top_candidate_text = scrapy.Field()
    top_candidate_score = scrapy.Field()
    all_candidate_chunks = scrapy.Field()

    failure_type = scrapy.Field()
    notes = scrapy.Field()
    pass
"""